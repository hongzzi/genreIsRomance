import React, { Fragment } from 'react';

import MovieForm from '../components/MovieForm';

export default () => {

    return (
        <Fragment>
            <MovieForm />
        </Fragment>
    )
}