import React, { Fragment } from 'react';

import MovieCard from '../components/MovieCard';

export default () => {

    return (
        <Fragment>
            <MovieCard />
        </Fragment>
    )
}